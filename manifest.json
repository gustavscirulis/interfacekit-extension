{
  "manifest_version": 3,
  "name": "InterfaceKit",
  "version": "0.1.0",
  "description": "Style any page in the browser, then copy the changes as a prompt.",
  "action": { "default_title": "Toggle InterfaceKit" },
  "permissions": ["activeTab", "scripting"],
  "background": { "service_worker": "background.js" }
}
